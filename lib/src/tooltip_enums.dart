enum TooltipDirection { top, bottom, left, right }

enum TooltipCaretDirection { left, center, right, none, custom }

enum TooltipProTriggerMode { tap, hold, tapAndHold }

enum TooltipAnimationType { none, fade, scale, fadeScale, slide }

enum TooltipAnimationCurve { easeIn, easeOut }
